# KIRTI Configuration — copy to .env and adjust as needed

# Server
KIRTI_HOST=0.0.0.0
KIRTI_PORT=8000
KIRTI_WORKERS=1
KIRTI_RELOAD=0

# CORS (comma-separated origins, * for all)
KIRTI_CORS_ORIGINS=*

# Camera index (default: 0)
# KIRTI_CAMERA_INDEX=0
